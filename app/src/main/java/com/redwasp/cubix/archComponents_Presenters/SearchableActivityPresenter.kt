package com.redwasp.cubix.archComponents_Presenters

import com.redwasp.cubix.arch.IPresenter

class SearchableActivityPresenter : IPresenter() {
    fun search(query : String){
        // get the string,
        // send to model, if there are search results,
        // update the view
    }
}